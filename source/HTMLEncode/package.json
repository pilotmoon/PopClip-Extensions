{
  "name": "htmlencode",
  "version": "1.0.0",
  "description": "HTML Encode extension for PopClip",
  "main": "_Config.js",
  "scripts": {
    "test": "npm run build && jsc Config.js",
    "build": "npx browserify _Config.js > Config.js",
    "install": "npm run build && open -a SignExt .",
    "buildugly": "npx browserify _Config.js | npx uglifyjs > Config.js"
  },
  "author": "Nick Moore",
  "license": "ISC",
  "private": true,
  "dependencies": {
    "html-entities": "^2.3.2"
  }
}
