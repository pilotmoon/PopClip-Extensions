{
  "name": "popclip-capitalize",
  "version": "1.0.0",
  "description": "",
  "main": "_Config.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx browserify _Config.js > Config.js",
    "buildugly": "npx browserify _Config.js | uglifyjs > Config.js",
    "install": "npm run buildugly && open -a SignExt ."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "sugar": "^2.0.6",
    "voca": "^1.4.0"
  }
}
