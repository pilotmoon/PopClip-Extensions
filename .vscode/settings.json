{
    "files.exclude": {	
        "**/*.js": {"when": "$(basename).ts"}
    },
    "search.exclude": {
        "*.popclipextz": true
    },
    "standard.validate": ["typescript"],
    "standard.engine": "ts-standard"
}